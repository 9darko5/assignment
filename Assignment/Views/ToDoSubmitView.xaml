<UserControl x:Class="Assignment.Views.ToDoSubmitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel Margin="10">
        <Label Content="Name" />
        <TextBox Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Priority" />
        <ComboBox ItemsSource="{Binding Priorities}" SelectedItem="{Binding SelectedPriority}" />
        <Button Content="Submit" Margin="0,10,0,0" Command="{Binding SubmitCommand}" />
    </StackPanel>
</UserControl>