<UserControl x:Class="Assignment.Views.LoadersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,10,8">
            <Run Text="Thread 1 progress: " />
            <Run Text="{Binding Threads[0].Elapsed}" />
            <Run Text=" / " />
            <Run Text="{Binding Threads[0].Duration}" />
            <Run Text=" s" />
            <Run Text=" (" />
            <Run Text="{Binding Threads[0].Progress, StringFormat={}{0:F0}}" />
            <Run Text="%)" />
        </TextBlock>
        <ProgressBar Grid.Row="0" Grid.Column="1" Height="20" Maximum="100" Value="{Binding Threads[0].Progress, Mode=OneWay}" />
        <Button Grid.Row="0" Grid.Column="2" Margin="10,0,0,8" Content="Cancel" Command="{Binding CancelFirstCommand}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,8">
            <Run Text="Thread 2 progress: " />
            <Run Text="{Binding Threads[1].Elapsed}" />
            <Run Text=" / " />
            <Run Text="{Binding Threads[1].Duration}" />
            <Run Text=" s" />
            <Run Text=" (" />
            <Run Text="{Binding Threads[1].Progress, StringFormat={}{0:F0}}" />
            <Run Text="%)" />
        </TextBlock>
        <ProgressBar Grid.Row="1" Grid.Column="1" Height="20" Maximum="100" Value="{Binding Threads[1].Progress, Mode=OneWay}" />
        <Button Grid.Row="1" Grid.Column="2" Margin="10,0,0,8" Content="Cancel" Command="{Binding CancelSecondCommand}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,8">
            <Run Text="Thread 3 progress: " />
            <Run Text="{Binding Threads[2].Elapsed}" />
            <Run Text=" / " />
            <Run Text="{Binding Threads[2].Duration}" />
            <Run Text=" s" />
            <Run Text=" (" />
            <Run Text="{Binding Threads[2].Progress, StringFormat={}{0:F0}}" />
            <Run Text="%)" />
        </TextBlock>
        <ProgressBar Grid.Row="2" Grid.Column="1" Height="20" Maximum="100" Value="{Binding Threads[2].Progress, Mode=OneWay}" />
        <Button Grid.Row="2" Grid.Column="2" Margin="10,0,0,8" Content="Cancel" Command="{Binding CancelThirdCommand}" />
        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,10,10,0" Text="Total progress" FontWeight="Bold" />
        <ProgressBar Grid.Row="3" Grid.Column="1" Height="20" Margin="0,10,0,0" Maximum="100" Value="{Binding TotalProgress, Mode=OneWay}" />
        <TextBlock Grid.Row="3" Grid.Column="2" Margin="10,10,0,0" Text="{Binding TotalProgress, Mode=OneWay, StringFormat={}{0:F0}%}" />
    </Grid>
</UserControl>
